apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: freight-ev-tls
  namespace: default
spec:
  secretName: freight-ev-tls-secret
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - freight-ev.aspire-computing.com
